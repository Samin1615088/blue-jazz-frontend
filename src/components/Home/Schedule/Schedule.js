import React from 'react';
import './Schedule.scss'

const Schedule = () => {
    return (
        <section className="vh-100 schedule-container">
            
        </section>
    );
};

export default Schedule;